node_modules/
.pnpm-store/
dist/
build/
coverage/
.next/
.next-*/
out/
.turbo/
.impeccable/
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/

# Claude Code local settings — contains this machine's approved permissions,
# which can include secrets typed into allowed command patterns (e.g. an API
# key passed inline to a CLI). Never commit.
.claude/settings.local.json

# Codex tooling — not used any more. (.agents/skills/ IS tracked: it holds the
# skills installed via `pnpm dlx skills`, exposed to Claude Code through the
# symlinks in .claude/skills/.)
.codex/
